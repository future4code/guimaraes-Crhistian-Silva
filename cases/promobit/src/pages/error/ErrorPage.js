import React from "react";
import { StyledError } from "./styleError";

export const ErrorPage = () => {
    return (
        <StyledError>
            <div>
            </div>
        </StyledError>
    );
};
